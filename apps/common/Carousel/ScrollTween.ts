import gsap from 'gsap';
import ScrollTrigger from 'gsap/dist/ScrollTrigger';

export const scroll = (node: HTMLElement) => {
    // ScrollTrigger.create({
    //     trigger:node,
    //     start:'top top',
    //     end:'bottom bottom',
    //     pin:true
    // })
}